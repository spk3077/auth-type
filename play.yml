- hosts: localhost
  tasks:
    - name: Testing Module
      authtype_info: 
        type: Basic
        webfile: ./webfiles/formatted/top500_formatted.txt
      register: result

    - debug: var=result    